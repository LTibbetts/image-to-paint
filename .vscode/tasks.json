{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Gradle Run",
      "type": "shell",
      "command": "cd ${cwd} && gradle installDist && cd ${cwd} && cd build && cd install && cd image2paint && cd bin && ./image2paint",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}